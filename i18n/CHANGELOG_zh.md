# 更新日志

## 2026-09-05 (第十四轮)
- 修复诊断管线在 GigaAM 引擎下必然失败：诊断器组装的 worker 重启状态缺 `gigaam_model_key` 键，ASR worker 本身加载成功后、应用激活 worker 时直接 KeyError 中止——任何用 GigaAM 引擎跑 `debug_pipeline.py` 的诊断都到不了识别阶段
- 修复诊断器的假阴性判定：运行报告在 `stop()` 之后才向写入器询问会话文件路径，而 stop 已经关闭并重置了写入器，于是「会话从未开启」的错误判定会把一次录制完全正常的诊断标为 FAIL——现在在停止前快照会话路径传给报告
- 端到端诊断真实通过（macOS/MPS，用户实际配置）：俄语语音 24.8s → VAD 切 4 段 → GigaAM 识别（含一次 interim 探针）→ HY-MT MLX 本地服务（2.5s 就绪）翻译为中文 4/4 → 四个视图按序落盘 → 封存 `session_status=completed`、`asr_engine="GigaAM (ru)"`（由本轮补齐的模型管理符号产出）。`--no-translate` 纯本地链路同样 Verdict OK

## 2026-09-05 (第十三轮)
- 修复应用完全无法启动的导入错误：主程序从模型管理模块导入的六个 GigaAM 辅助符号（默认模型键、归一化、显示名、俄语单语判定、模型/修订版 id）从未随模型管理模块一并提交——主程序、全部依赖它的测试模块在导入时即崩溃。现已在模型管理模块补齐这组符号：GigaAM-v3 配置档案（`e2e_rnnt` 修订版、俄语单语）、旧设置拼写/裸仓库名/引擎名别名归一化、未知键安全回退到默认档案
- 修复进行中会话的元数据 sidecar 首次落盘时 `session_status` 为空：状态位此前在写完首份 sidecar 之后才设置，记录中心按此字段识别「仍在录制」的会话，首份 sidecar 上的空值会被当作旧格式回落到页脚启发式。现在创建 sidecar 前先置 `active`，首份提交即携带正确状态
- 记录列表刷新少读一份 JSON：会话枚举的元数据 glob 此前把 AI 纪要的 sidecar 也读进来再按文件名丢弃；现在读取前即按既有的纪要文件名后缀跳过——每次列表刷新每个已总结会话少一次完整 JSON 读取（与第十轮「纪要元数据单次读取」的目标对齐）
- 测试套件首次完整运行并全绿（463 通过/4 跳过）：把 26 个停留在旧契约的用例更新到当前契约（入队条目携带会话身份的五元组、pause 的队列序清理标记、孤儿翻译按文档化为丢弃、通配写入以原文文件即时可见、live 角色以写入器为准、stale 徽标需正常完结记录、删除断言按存在性而非候选枚举、interim 桩补齐会话身份参数、补缺失的 pytest 导入）

## 2026-09-05 (第十二轮)
- 把上一轮「临时菜单/对话框 exec 后安排销毁」的修复推广到全应用同模式入口：字幕悬浮窗的右键菜单（复制/导出/清空）、字幕行设置的行编辑对话框（编辑/新增）、设置面板的模型下载/新增/编辑对话框、切换 ASR 引擎时的下载与加载对话框——这些同样是每次调用新建、以父窗口持有 C++ 所有权、`exec()` 结束只隐藏不释放的对象，反复操作会在父窗口上累积驻留。现全部在 `exec()` 返回后的 `finally` 中安排 `deleteLater()`，取消/正常选择/异常路径都覆盖，业务派发先于销毁安排完成；行为不变。首启向导等一次性进程级对话框（无父对象或每进程仅一次）不受影响，保持原样

## 2026-09-05 (第十一轮)
- 修复记录中心临时菜单与编辑对话框的累积驻留：右键列表、点导出按钮弹出的菜单以及纪要编辑对话框都是每次调用新建、并带页面作为 Qt 父对象——Qt 的父子所有权让它们的 C++ 对象归页面持有，`exec()` 结束只是隐藏并不销毁，Python 引用出作用域后也不释放，于是每次右键/每次点开导出菜单都给页面留下一个菜单（含其 QAction 与 Python 闭包）、每次打开编辑器留下一个对话框树，直到页面销毁才回收。现在三处都在 `exec()` 返回后的 `finally` 中安排 `deleteLater()`，取消、正常选择与异常路径全覆盖；业务派发在销毁安排之前完成。菜单/对话框的行为不变
- 静态测试补充（未执行）：两处临时菜单在每次调用后都安排了销毁（桩 QMenu 记录实例与 deleteLater 调用，不依赖 Qt 子对象计数或事件循环时序）

## 2026-09-05 (第十轮)
- 打开「会议记录」页不再刷新两次：此前每次切到该页都会先经注入入口安排一次延迟刷新、同步又完整刷新一次，读同一目录两遍。现在注入入口对未变化的写入器幂等（不再安排延迟刷新），同步刷新会取消尚未触发的延迟刷新——首次进入、重复进入、运行中重新注入三种路径都恰好一次完整刷新
- 暂停/恢复/结束录制不再扫描全部历史会议：此前管线的每次暂停、恢复与「正在结束」推送都会完整重读目录——逐会议读取纪要正文与元数据、校验哈希、必要时整篇解析记录再算源哈希，成本随历史会议总量增长且全部发生在界面线程。现在目标会话已在列表缓存中且为活动行时，状态推送只按会话编号原地更新该记录的内存标记（活动/结束/中断位），从同一份快照重建行并原地同步详情徽标与按钮——零读盘、零纪要重读、零哈希；正在生成的总结不受影响。会议封存完成（IDLE）仍完整刷新一次并定位刚结束的会议；新会议的首次 ACTIVE、身份缺失或缓存与状态矛盾时保守回退一次完整刷新。ENDING 期间记录不会被误标「已中断」，暂停/恢复不改动过期/已编辑等磁盘派生标记
- 静态测试补充（未执行）：同一写入器重复注入不安排延迟刷新、写入器真正变化只刷新一次且同步刷新取消挂起的延迟定时器、页签进入路径恰好一次刷新；暂停与恢复推送零目录扫描、零完整刷新且磁盘派生字段不变；ENDING 推送零读盘且只标记目标会议（不误标中断）；新会话 ACTIVE 回退完整刷新；IDLE 完整刷新并选中目标（无身份的 IDLE 也完整刷新）；轻量推送不取消运行中的总结线程；stale/edited 徽标轻量更新前后保持

## 2026-09-05 (第九轮)
- 修复「暂停」丢失最后的缓冲短句与回声去重：暂停路径此前在「本次 flush 没有剩余音频」时立即清空增量识别状态，但这只证明本次暂停没有新段要交，不证明识别队列里没有此前已入队、尚未消费的 vad_flush（VAD 自然切分、静音喂入、更早的移交都会入队）——该条目随后被消费时读到已清空的 `_interim_active`，走普通识别路径：已缓冲的短句（音频早已裁掉、文字是唯一副本）从未写入，echo tail 也失效。现在暂停不再自行清理：整个「flush → 入队 → 清理判定」在会话边界锁（生产者 fence）内完成，音频成功入队时由消费端既有清理复位；没有音频入队时向队列尾追加一个不带音频的清理标记，识别线程是单消费者 FIFO，标记严格排在暂停前所有条目（含仍在写入状态的增量识别趟）之后、恢复后的新音频之前，由标记处理完的既有清理完成唯一一次复位——「暂停前残余语音完成处理、恢复后从全新话语开始」由顺序保证而非提前清理。标记本身无法入队时（退出已开始、代次关闭、队列连丢弃一个后仍拒收）才由暂停线程就地清理兜底。Qt 主线程全程只做短本地操作（VAD flush、非阻塞 put、计数登记），从不等待队列排空
- 静态测试补充（未执行）：旧 vad_flush 已排队且暂停 flush 返回 None 时状态不被清空、缓冲短句拼接落盘且 echo 剥离生效；无旧消费者且无剩余音频时标记兜底复位且计入等待计数；暂停→恢复后新话语不拼接旧 pending、不被旧回声尾误剥；ASR 未就绪时状态仍归旧消费者、标记排在其后；退出已开始时两次入队均被拒、就地清理不泄漏。同时修正既有静态测试中 pending 字面量与拼接断言的空格不一致（拼接为逐字拼接，"Да," 不含尾随空格，断言里的 "Да, Хвост" 需要 pending 自带空格才能成立）

## 2026-09-05 (第八轮)
- 修复「暂停后立刻结束录制」或「自然切分后立刻结束录制」丢失最后的缓冲短句：结束收尾的 flush 在「本次没有剩余音频」时立即清空增量识别状态，但这只证明本次 flush 没产生新段，不证明识别队列里没有此前已入队、尚未消费的 vad_flush（暂停移交、VAD 自然切分、静音喂入都可能在结束前入队，且持有同一会话的等待计数）——识别线程随后读到已被清空的 `_interim_active`，把这段音频送进普通路径：已缓冲的短句（音频早已裁掉、文字是唯一副本）从未写入会议记录，回声去重也随之失效。现在 flush 在任何路径（包括音频未能入队的情形）都不再清理，唯一的复位点移入结束线程的队列排空阶段：先只等本代次的队列工作排空（不先等网络翻译），然后复位，再在同一个 30 秒预算内等翻译收尾——两段等待共享同一单调期限，结束总上限不变；队列等待超时或收尾本身异常时仍然复位（有日志警告，滞留者随后被代次守卫拒绝），脏状态不再泄漏进下一场会议
- 静态测试补充（未执行）：已有 vad_flush 排队且本次 flush 返回 None 时状态不被清空、消费端正确走增量收尾并把缓冲短句拼接落盘；未入队路径（识别未就绪、代次失效、退出已开始）把复位留给排空阶段；无旧工作且无剩余音频时复位仍会发生且封存照常完成；复位严格发生在队列消费端完成之后、封存之前；队列等待超时时结束不悬挂、状态仍复位、滞留条目的事后释放幂等

## 2026-09-05 (第七轮)
- 修复结束录制丢失最后的缓冲短句、并重复已提交句尾：结束收尾此前在把最后一段 VAD 音频交给识别线程后立即清空增量识别状态——而识别线程正是靠这个状态决定走「增量收尾」还是普通路径的（读到 `_interim_active` 已被清为否），于是最后一段走错了普通路径：已缓冲的短句（其音频早被裁掉、文字是唯一副本）从未写入会议记录，缓冲里残留的已提交句尾也会原样重复写入一遍。现在这段音频成功入队时生产端不再清理，缓冲短句、回声尾与分发标志随段一起移交，由识别线程处理完后的既有清理统一复位（与「暂停」的移交语义一致）；仅在音频未能入队的情形（识别未就绪、没有剩余音频、代次已失效、退出已开始、队列拒收）才由结束线程就地清理
- 静态测试补充（未执行）：结束时缓冲短句拼接进最终记录、成功入队后移交前 pending/回声尾/分发标志不被清空、识别线程处理后状态仅复位一次且等待计数排空、四种未入队路径立即清理、回声尾使最终重识别剥离已提交前缀不重复落盘

## 2026-09-05 (第六轮)
- 修复识别期间点「开始新记录」会把旧语音写进新会议：过期代次守卫此前只在条目处理开始时检查一次，而识别本身可能耗时数秒——守卫通过后用户显式开始新录制，旧音频的写入带着「无预期会话」（legacy 通配语义）执行，写入器无法拒绝，开场前的语音就落进了新会议的文件。现在最终写入前在会话边界锁内重新校验代次，「重校验 → 写入（含可能的自动开会话）→ 计数登记 → 收养」与显式开始成为同一个线性化区段：显式开始先完成则旧音频被拒绝，写入器已先自动开会话则完成同一场收养（不产生双代次或双次 ACTIVE）；锁内只做短本地操作（信号发射、缓冲文件追加、计数集合操作），识别与翻译提交仍在锁外
- 修复收养辅助函数的提前返回漏掉计数迁移：函数入口处「已有会话在录就直接返回条目代次」的判断不比较代次——条目按旧代次 N 登记后、显式开始抢先翻到 N+1 并清理 N 的计数时，函数直接返回 N，这条翻译仍按已失效的 N 释放提交，新会议的结束等待永远不会等它，收尾会把这句封存为「未翻译」。现在所有分支都在边界锁内比较代次：同代次走明确身份条件的快速路径，异代次（显式开始认领了该条目所在的写入器会话）则把计数恰好一次地迁移到当前代次
- 修复增量识别被拒时永久丢失缓冲短句：`_do_interim_asr` 此前先把缓冲短句拼进句子并立即清空，身份守卫一旦拒绝（代次过期或会话不符），这些短句既没写盘、其音频又早已在上一轮被裁剪，永久丢失。现在拼接前保留快照、拒绝时恢复、提交确认成功后才清空；裁剪、回声尾与增量状态只计入已确认消费的文本，多句中前几句已提交、后句被拒时只消费已提交前缀
- 静态测试补充（未执行）：识别期间显式 begin 在 fence 重校验被拒、识别期间 end+begin 的旧条目被拒、写入后 begin 抢先时 helper 计数迁移且新代次等待覆盖、pending 拼接后被拒必须恢复且后续提交恰好一次、多句前缀成功后句拒绝只消费前缀

## 2026-09-05 (第五轮)
- 修复删除同秒记录会连带销毁兄弟会议：删除一场记录此前按文件名前缀匹配（`livetrans_{编号}_*`），而裸编号恰好是同秒后缀兄弟（`_01`）文件名的前缀——删除历史记录 A 会把同秒开始的会议 B 的原文、译文、合并文本、Markdown、元数据和 AI 纪要一并删掉（若 B 正在录制，macOS 上记录器仍写入已删除的文件，整场静默丢失）。现在按精确文件名枚举删除（写入器的文件种类定义 + 纪要对 + 各自的 `.tmp*` 暂存兄弟），只删用户确认的那一场；删除后缀兄弟同样不影响裸编号
- 修复自动开会话丢开场语音：不点按钮、直接开管线说话时，第一个条目自动开会话并入状态机的瞬间会把会话代次 +1，导致在此之前已入队、本属同一场会议的语音被「过期代次」守卫整体丢弃（增量识别路径还会照常裁剪音频，永久丢失开场句子）。现在收口为唯一的 adoption 辅助函数，复用当前代次不再跳变；会话开始前入队的条目从入队起就计入等待计数（结束收尾会等它们），被身份守卫拒绝的 interim 句子不再被当作已消费（不裁剪、不进回声尾，留给后续提交）；显式开始/结束的旧代次隔离规则保持不变，「开始新记录」与自动收养竞争时只产生一个权威会话与一次状态通知
- 静态测试补充（未执行）：删除裸编号后逐文件断言 `_01` 兄弟幸存且内容不变、反向删除互不影响、删除枚举与纪要层文件名防漂移；adoption 前入队的两条均落盘、adoption 后立即结束会等待在途工作、多句 interim 首句收养后其余不丢、整趟被拒不裁剪、显式 begin 仍拒绝会话前旧音频、仅字幕不落盘、adoption 与显式 begin 竞争单一权威

## 2026-09-05 (第四轮)
- 搜索与筛选不再触发详情重载：此前在搜索框每敲一个字符（或切换一次筛选）都会在保留选区的同时重新读取该会议的纪要文件、重渲染 Markdown 并重建供应商/语言选项——数据其实没有任何变化。现在搜索/筛选只从既有列表数据重建行；手动刷新、会话状态变化或生成完成时才同步详情数据，当前标签、记录视图滚动位置与正在运行的总结线程均不受影响；选区被筛掉时的回退选中仍正常加载详情
- 记录列表读取纪要元数据减半：列表刷新时每个带纪要的会话此前会把同一个元数据 JSON 读两遍（纪要对校验读一次、紧接着又重读一次）；现在直接复用校验通过的那份元数据，一次列表刷新每个会话只读一次，编辑/过期判定也因此与哈希校验的是同一份数据
- 会议结束后的自动定位只加载一次详情：此前会先把旧选中会议的详情原地刷新一遍、再加载刚结束的目标会议；现在先把目标会话定下来，详情只更新一次
- 记录器内部「仍持有会话资源」的六项判定收敛为一个锁内私有谓词：IDLE 广播门槛与记录中心的精确文件归属查询共用同一实现，不再存在两份可能各自漂移的条件副本
- 静态测试补充（未执行）：搜索/筛选零读盘、筛掉选区后的回退加载、目标会话单次详情加载、纪要元数据单次读取

## 2026-09-05 (第三轮)
- 堵死结束确认框的竞态:结束请求现在把目标会话编号一路传到应用层权威入口,在真正切换到「正在结束」的前一刻原子复核——确认框会运行嵌套事件循环,期间旧会议可能已结束、新会议可能已开始,此前确认后调用的是无参结束方法,会结束「确认后碰巧在场的那场」。悬浮窗的结束按钮同样在确认框打开前固定目标身份;身份不符时提示而不执行
- 修复重命名触碰已销毁的列表行:重命名弹窗期间一次刷新会清空并销毁旧行对象,成功后继续对旧行调用更新会直接崩溃;现在按会话编号重新定位当前行,且弹窗期间会议被删除时拒绝重命名(不再为已删除的文件重建元数据、留下幻影记录)
- 修复文件归属的子串误判:记录器新增锁内的精确归属查询(正在持有哪个会话的文件),替代按路径子串判断——裸会话编号是其同秒后缀兄弟(如 _01)文件名的前缀,子串匹配会把删除父记录误拒
- 纪要保存真正串行化:UUID 暂存名只防重名,防不了两次保存的最终替换交错成「正文 B + 元数据 A」(随后哈希校验把两者一起判为不存在);现在数据层用模块锁串行整个双文件提交,并如实声明该锁仅进程内有效。暂存写失败时清理自身临时文件;编辑保存前若原纪要已被删除或损坏,视为冲突拒绝,不再静默复活旧内容
- 修复保留选区后详情停留旧数据:列表重建保留选区时,详情的标题、元信息、会议信息与纪要现在原地刷新——不重置记录视图滚动、不切换标签、不打扰正在生成的总结;「正在保存」的记录导出按钮在界面层直接禁用(handler 保留最终权威校验)
- 静态测试补充（未执行）：结束入口身份复核、重命名弹窗期间刷新/删除、父戳与后缀兄弟的归属判定、并发纪要保存一致性、暂存失败清理、纪要消失冲突、ENDING 导出按钮禁用、保留选区的详情原地刷新

## 2026-09-05 (第二轮)
- 修复右键菜单删除错目标：在未选中的历史记录上右键删除时,此前实际删除的是当前选中行（删除处理读取「当前项」而非右键指向的记录）。现在删除目标显式携带右键所指向的记录,身份贯穿确认框与最终删除;详情面板的删除按钮同样显式传入选中记录
- 操作许可统一收口：生成/编辑/删除/导出/结束按钮现在在同一处按所选记录状态刷新（此前仅结束按钮参与,其他按钮界面显示可点但点击后被拦截）。总结运行中,生成与编辑按钮保持禁用,任何状态推送都不会误重新启用它们
- 操作前改查权威状态而非缓存快照：删除、生成纪要、结束记录、重命名在执行瞬间重新询问记录器的活动/收尾会话,不再依赖上次刷新缓存的记录标记——刷新后才开始的会议（缓存仍显示为历史记录）不会再被误删或误生成纪要
- 结束请求携带会话身份：页面的结束请求信号现在携带目标会话编号,应用侧在执行前核对该编号仍是记录器当前会话（竞态下会议已结束或已开新场时拒绝执行并提示）,不再「点到即结束当前任何一场」
- 重命名闭合身份链：记录器的重命名接口新增期望会话参数,边写边校验——弹窗期间发生了结束+开场竞态时,新会议不会继承旧会议的标题
- 修复「正在保存」与「异常中断」并存：收尾窗口内的记录此前同时被判为 is_ending 与 interrupted,后续筛选、导出、许可会把它误读为崩溃残留;两者现在互斥
- 临时文件名跨模块唯一：记录器与记录数据层此前各持一个独立计数器却共用同一命名格式与同一目标文件,同进程可生成相同暂存路径互相替换;两处及纪要保存全部改用 UUID 后缀,并发保存（生成与手动编辑同时落盘）不再互相覆盖
- 修复 AI 纪要编辑竞态：编辑器打开时固定会话身份与原始内容哈希,保存前校验（选区被后台刷新切换、或生成恰在弹窗期间完成时拒绝保存并提示,不再静默写入另一场会议或覆盖新生成的内容）;总结运行期间编辑按钮禁用
- 收尾期间禁止导出：封存仍在写最终条目与脚注时导出会得到缺尾的「完整记录」;现在拒绝并提示稍候（活动记录导出仍允许,并在 PDF 中明确标注为快照;收尾快照同样标注状态行）
- 修复刷新打断总结生成：列表每次重建都会把选区重置到第一行并触发详情重载,重载会取消正在运行的总结线程——搜索、筛选或任何状态推送都会误杀生成。现在选区按会话身份跨重建保留,仅真正的会话切换才取消生成
- 修复多组无效测试（未执行）：9 处在会话打开前读取会话编号的断言（读到 None,运行必错）、`or True`/`if False` 恒真占位断言、`.tmp` 遗留检查漏掉新命名格式;补充右键目标、缓存竞态、期望会话、编辑错会话、暂存名碰撞、收尾导出等测试

## 2026-09-05
- 修复结束状态错绑到所有记录：此前「正在结束」按全局状态展示,任何一场会议收尾时,列表中全部历史记录都会被标成「正在保存」。现在结束状态携带会话编号,只有正在收尾的那一场显示该状态（徽标、详情、操作许可全部按记录身份判断）
- 修复结束按钮出现在错误记录上：按钮只出现在当前活动会议自己的详情里;点击时再次校验所选记录是否仍是活动会议（竞态下按钮已失效则不发送结束请求）;历史记录详情不再显示该按钮
- 修复收尾期间可能生成 AI 纪要：收尾执行窗口内该记录不再被标记为「正在记录」,此前的拦截条件因此短路放行;现在拦截同时检查「正在记录」与「正在收尾」两个记录级状态
- 增加活动/收尾记录的删除防护：删除前在底层再次校验（界面禁用仅为提示）,正在记录或收尾的会议无法删除——macOS 上删除仍被占用的文件会让记录器继续写入已删除的 inode,最终整场丢失;拒绝时给出本地化提示
- 消除会话元数据的并发双写者：活动会议的重命名统一改走记录器锁内接口（标题随封存的元数据合并存活）,收尾期间禁止重命名;已结束记录保留原路径。两处原子写的临时文件改为每次唯一命名,不再互相替换
- 静态测试补充（未执行）：结束状态只标记自身行、活动记录删除被拒、结束按钮仅在活动会议详情出现且竞态点击不发送、活动会话经记录器重命名、收尾期间拒绝重命名

## 2026-09-04 (第七轮)
- 修复测试文件的机械替换事故：7 处「stamp = stamp」自赋值（保存语句本身被上轮批量替换误改）,恢复为 close 前保存会话编号；全文扫描确认无同类自赋值或未定义变量
- 修复「completed 标记早于内容落盘」的封存顺序缺陷：此前状态元数据在文件句柄 flush/关闭之前写入,句柄随后失败会在磁盘留下错误的 completed 提交标记。现在顺序为：条目 → 脚注 → 内容文件 flush+fsync+close → 判定状态 → 最后原子提交元数据；任一步失败只能提交 interrupted
- 内存清理不再作为封存依据：内容封存（返回成败）与状态清理（仅善后）拆分,失败证据（磁盘文件与状态）在判定前不被清除
- 加固「结束完成」的判定：新增最严格的资源检查（逻辑开、封存中、已开标记、存活句柄、编号或路径任一存在即未收口）,覆盖「已开标记为真、会话标志为假」的半死状态；广播空闲前以此为准
- 应用退出路径的 close() 与按钮结束共用同一套失败收口（标记 interrupted、释放句柄、清理状态）且幂等,异常不再外抛打断停止流程
- 写入异常边界修正：已死句柄产生的 ValueError 与 OSError 同走 interrupted 路径,普通编程错误仍向上传播
- 静态测试补充（未执行）：completed 元数据晚于内容文件关闭、fsync 失败判 interrupted、半死状态被资源检查识别、close 异常后可开下一场

## 2026-09-04 (第六轮)
- 修复「写入失败的条目重新出现」：原文行写盘失败时此前仍残留待处理队列，结束收尾会把它当作未翻译条目再次输出；现在失败即完整回滚（内存无痕迹、语音时长不计数），结束统计只反映真正落盘的条目
- 会话状态显式持久化：会话元数据新增 session_status 字段——开启即写 active,完整封存（全部文件与最终元数据都写成功）才提交 completed,中止或任一必需写入失败标记 interrupted;旧格式记录（无此字段）继续按脚注/结束时间兼容判断
- 修复「半封存被误判为正常结束」：此前脚注已写、元数据写失败再中止的记录,会因脚注存在被当作正常结束；现在新格式的 interrupted 状态优先于脚注——只有 status=completed 才算正常结束
- 中止路径加固：abort 全程不向外抛出（状态清理放在 finally）,并把中断标记尽力写回元数据；主流程只有在确认记录器已无活动会话后才广播「空闲」,不再存在「writer 状态未知却声称收尾完成」的分支
- 静态测试补充（未执行）：原文写失败内存回滚、脚注/最终元数据写失败判为 interrupted、带脚注的 interrupted 不被误判、中止后可正常开下一场、旧记录无 session_status 的兼容判定

## 2026-09-04 (第五轮)
- 修复一处阻断级语法错误（记录汇总函数存在重复定义与错误缩进,模块无法导入）
- 会话文件组改为真正的原子创建：文件头写入失败立即关闭并删除该文件;初始元数据纳入成功条件（独占创建,消除「先查后写」的覆盖竞争）,任一失败整体回滚,开始记录不再返回虚假成功
- 彻底堵死跨会话串写：条目写入返回明确状态（已记录/仅字幕/会话不匹配/写入失败）,仅已记录的条目才登记翻译计数并提交翻译;旧会话条目被拒后其晚到翻译直接丢弃;「无原文译文」写入当前会话的旧路径删除（这正是串写通道）;仅字幕模式照常识别翻译但不写任何会议文件
- 修复结束边界的尾句丢失：采集线程的「闸门检查→VAD→入队」与结束线程的「闸门→封场」现在在同一把会话边界锁上线性化——结束前 VAD 已接收的语音必定入队保留,结束后的音频不再进入 VAD,不再以「确定丢弃」代替保存
- 结束失败时中止会话：封存中途出错会释放文件句柄、清理内存状态、不追加任何内容（该场记录按「异常中断」归类）,状态机只有在记录器已封存或已中止后才进入空闲,不会出现「界面空闲但记录器仍有活动会话」
- 静态测试修正：close 前保存会话编号（封存后不再引用已清空字段）、新增「被拒原文的晚到翻译不进新会话」「元数据创建失败整体回滚」「结束失败中止后记录保留且可重开」三组断言（未执行）

## 2026-09-04 (第四轮)
- 修复同秒开两场会议的文件冲突从未生效的问题：会话编号的冲突探测此前检查的是没有扩展名的路径（实际文件都带 .txt/.md/.json 后缀）,探测永远落空,第二场会议撞上独占创建后以「半开文件组 + 虚假成功」收场。现在按真实文件名探测,同秒第二场拿到 _01 后缀；文件组创建改为全有或全无——部分失败即回滚（关闭并删除已建文件,不返回虚假会话）,首份元数据只允许独占创建,任何失败路径都不会覆盖已有元数据
- 修复会议结束的尾句丢失与写入身份校验：识别队列的每一项现在同时携带会话代次与入队时的会话编号,写入前在记录器锁内再次核验会话身份——旧音频的条目进不了新会话的文件；收尾线程对冲刷、等待、封存全程容错,任何一步失败都保留既有记录并照常离开「正在结束」状态（不再有卡死的结束态）；封存后推进代次,超时晚到的任务明确失效；解除采集闸门之前先置暂停,消除「结束后短暂继续监听」的窗口
- 修复会话元数据的时间语义：活动会话的元数据不再携带结束时间（此前每次中途改写都会写入一个实时结束时间,崩溃遗留的半场记录因此被误判为正常结束）；结束时刻在封存时只生成一次,正文脚注、元数据与返回的摘要共用同一时间戳
- 修复 PDF 分页：此前每一页都重复绘制第一页的内容（分页裁剪矩形始终指向文档顶部）,且正文与页脚无预留空间互相覆盖。现在文档按「页高减页脚预留」布局,每页平移到自己的切片绘制,页脚拥有独立的 12mm 底部条带
- 修复 AI 纪要的提交与取消语义：孤立正文（元数据缺失或损坏）不再被当作已提交纪要——元数据是提交标记,没有它就没有纪要；无哈希的旧版纪要继续视为有效；取消与保存现在在同一把锁上线性化,已确认的取消不会再被随后完成的保存覆盖,完成的保存恰好公告一次；任务注册表的清理改用 isFinished 判定,不再误删「已注册未启动」的线程引用

## 2026-09-04 (第三轮)
- 修复结束收尾的最后一个登记竞态：识别工作追踪与「停止接收新工作」此前分别用不同的检查,采集线程在「刚生成片段、还没入队」的瞬间点结束仍可能在等待开始后补登记,让收尾提前封存或产生幽灵写入。现在每个会话代次在追踪器内有明确的 OPEN→CLOSING→SUPERSEDED 生命周期,状态切换、登记与归零判定在同一把锁上线性化：普通采集片段在进入 CLOSING 后一律拒绝（要么在切换前已计数、被等待并保留,要么切换后被丢弃——不存在中间态）；收尾的最后一次 VAD 缓冲冲刷走独立受控入口登记；等待基于 Condition 阻塞,不再依赖队列状态、轮询或 sleep
- 修复跨会话串写：识别队列的每一项现在携带其登记时的会话代次,识别与翻译的登记、释放、写入守卫全部按这个代次进行——结束后立刻开始新会议时,旧队列残留的片段不会再写进新会议的文件,旧翻译的计数也不会挂到新会议的收尾等待上
- 修复仅字幕模式被误伤：「无会话」与「会话已结束」现在区分处理——恢复实时字幕但不记录时,识别照常运行、只是不计数不等待;会话已结束的片段则被丢弃。遗留的自动开会话路径（未点过按钮的用户的既有行为）纳入追踪器,结束该会话时同样等待其识别与翻译
- 修复会话追踪状态的累积：已结束会话的登记项在排空后即回收,长时间多次录制不再线性堆积
- 修复 AI 总结取消时客户端泄漏：取消落在客户端创建之后、首个请求之前时,连接池此前无人关闭;现在取消、失败、崩溃与保存完成各终态都会关闭本次运行的客户端（重复关闭无害）

## 2026-09-04 (第二轮)
- 修复结束记录的收尾竞态：此前用「ASR 队列为空且没有在途翻译」判断收尾完成，但识别线程可能已取走任务——队列为空、识别还没回来，结束线程就提前关会话、丢掉最后一句话。现在每段音频进入识别队列前就登记为所属会话的工作，识别中、等待翻译都算未完成，只有成功、被过滤、失败等终态才释放；结束在后台等待这个计数归零（最多 30 秒），界面全程不卡
- 已结束的会议不再被后台结果修改：此前超时后返回的翻译会重新打开旧会议文件追加，破坏 Markdown 顺序、Summary 尾部结构、统计和 source_hash。现在收尾超时的条目按原文保存并标记未翻译，end_session 完成后本场文件彻底不可变——晚到的识别或翻译结果直接失效，不会重开文件、不会生成幽灵会议。若以后需要补译，会做成独立的「重新翻译历史记录」功能
- 结束记录后应用停止监听：点击「结束本次记录」完成收尾后，音频采集与实时翻译保持暂停（ASR 模型留在内存、不卸载、不重启），不再出现「记录已结束但应用还在识别和调用翻译 API」的状态。点「开始新记录」会先创建新会话、再恢复监听；开始失败不会留下空的活动会议。期间在悬浮窗点「继续」会询问是否同时开始新记录，选「否」仅恢复字幕（不保存）
- 修复收尾期间悬浮窗状态错配：结束中不能重复点暂停或开始（避免与收尾线程抢 VAD 缓冲）；会议显示空闲但悬浮窗仍显示「运行」的情况已同步；结束完成后悬浮窗与托盘一致显示暂停
- 收口 AI 总结线程退出：总结任务注册表改由应用对象持有（不随页面或 main() 栈帧销毁）；取消时除协作标志外还会关闭底层 OpenAI/httpx 客户端，把在途请求尽快打断（而非等满 120 秒超时）。说明：关闭传输是「中断」不是「杀死」——卡死 socket 上的请求仍需片刻才能浮出错误，这是同步网络请求的固有限制，未宣称完全解决
- 修复收尾阶段悬浮窗监视条引用未计算变量的潜在崩溃（rms 在定义前使用）

## 2026-09-04
- 新增独立的「结束本次记录 / 开始新记录」功能：会议记录从此不再只能靠退出应用来收尾。悬浮窗标题栏新增一个随状态变化的按钮——正在记录（绿色「结束本次记录」）、已暂停（琥珀色，仍属同一场，可继续）、正在结束（禁用的「正在结束…」）、空闲（灰色「开始新记录」）。点「结束」会先确认：结束后本场保存进会议记录、可生成 AI 纪要或导出 PDF,继续记需要开新一场。结束只收记录,不关窗口、不卸载 ASR 模型、不打断翻译管线；结束后自动刷新会议记录中心并选中刚结束的那场，AI 纪要的「正在记录」限制随之解除。同秒连开两场时新会话文件名自动加 `_01`/`_02` 后缀，绝不追加写进上一场的文件
- 新的会话收尾流程：结束时在后台线程依次完成——暂停接收新语音、把 VAD 里最后半句交给识别、等待在途识别与翻译完成（最多 30 秒）、把没等到的内容按原文保存、写入最终元数据并关闭本场文件，全程不卡界面。每条记录都绑定所属会话：结束后才返回的翻译会写回它自己的那场会议，不会污染下一场、不会生成幽灵记录
- 会议记录中心新增状态区分：正在记录 / 已暂停 / 正在保存 / 异常中断（上次崩溃或强退遗留的半场记录明确标注，不再伪装成正常结束）。正在记录、已暂停和正在保存的会议仍不能生成最终 AI 纪要；记录页也提供「结束本次记录」按钮，与悬浮窗走同一条结束路径
- 修复三个生命周期问题：页面销毁后可能继续执行的裸 `QTimer.singleShot` 回调改为页面持有的单次定时器；`main.py` 直接改 `panel._transcript_writer` 私有字段改为公开注入接口（注入后记录页立即刷新活动状态）；AI 总结线程不再「挂到 QApplication 了事」——新增应用级任务注册表持有强引用、页面销毁后继续存活、统一在退出时协作取消、结束后恰好清理一次，全程不使用 `terminate()` 也不在主线程长时间等待

## 2026-09-03
- 新增「会议记录中心」：设置页的「会议记录」从单行文件列表重构成主从结构的记录中心——左侧是可搜索、可按「全部/已总结/未总结」筛选的会议列表，每场会议显示标题（可双击或右键重命名）、时间、时长、条数、翻译模型和 AI 纪要状态；右侧详情默认展示 AI 纪要，也可以切到「完整记录」（时间戳、译文为主体、原文可折叠、未翻译条目明确标注、长会议分批渲染不卡界面）或「会议信息」（时长、有效语音时长、条数、引擎、语言等全部元数据）。布局用 QSplitter 自适应宽窄窗口，分隔位置会记住
- 新增 AI 会议纪要：一键把整场会议记录发给所选模型生成结构化 Markdown 纪要，提供「会议纪要」和「课堂笔记」两个模板（课堂笔记含知识点、定义公式、作业截止时间等小节），输出语言可跟随会议语言或手动指定。45～120 分钟的长会议自动按发言边界分块提取再汇总，界面实时显示「正在分析第 n/N 部分」，后台线程运行不会卡住界面，可随时取消；供应商从你已配置的模型列表中选择且与实时翻译模型相互独立（按稳定 ID 记忆，删除模型不会串）。纪要保存在 transcripts/ 目录（`livetrans_{会话}_summary.md` + 元数据），重启后仍在；记录更新后旧纪要标记「建议重新生成」而不是删除。生成前首次使用云端供应商会明确提示记录将被发送给第三方
- 新增 PDF 会议纪要导出：导出菜单提供「仅 AI 纪要」和「AI 纪要 + 完整双语记录」两种 A4 PDF，含标题、会议元数据、页码，中文/俄语/英文正常渲染、文字可复制可搜索；没有纪要时也可导出完整记录 PDF，不会暗中把记录发上云端。原有 Markdown、TXT 导出整合进同一菜单
- 纪要可直接编辑保存：手动编辑过的纪要带「已手动编辑」状态，重新生成前会确认，避免覆盖你的修改
- 修复纪要保存的半提交状态可能造成「新元数据配旧正文」的问题：纪要正文与元数据此前分两次替换，中途崩溃会让 provider、生成时间与实际内容对不上；现在两份文件先写临时文件、提交时先替换正文、最后替换带正文哈希的元数据——读取时校验哈希，不匹配就当作无纪要而不是展示错配的内容，重新生成即可恢复
- 修复会话进行中重命名会议后标题丢失的问题：记录写回元数据时此前整个重写文件，把用户改过的标题一并抹掉；现在按白名单保留标题字段，会话结束、引擎切换都不会再覆盖
- 会议列表新增「正在记录」状态：仍在进行中的会议明确标注，其条数/时长的快照可查看、可导出（PDF 会标记记录进行中），但不能生成最终 AI 纪要——避免把半场快照误当成完整纪要
- 修复宽窗口下会议记录页误入窄屏布局的问题：窄屏阈值此前按整个设置面板的宽度判断，扣掉左侧导航栏后 900px 面板实际只剩约 700px，恰好触发堆叠模式；现在按页面自身可用宽度判断
- 修复切换会议或重新生成后旧总结任务的结果可能晚到覆盖新纪要的问题：每个总结任务绑定会话与代次编号，旧任务的结果直接丢弃；单次请求超时从 300 秒缩到 120 秒，关闭窗口不再需要等一次挂起的请求自然结束
- 修复复制/编辑模型会破坏总结供应商记忆的问题：复制模型不再沿用原模型的稳定 ID，编辑模型保留原 ID，供应商按 ID 记忆因此不会串到别的条目上
- 修复翻译专用模型的输出上限会截断纪要的问题：纪要请求的 max_tokens 不再继承实时翻译模型的配置（如 HY-MT 的 128），保底 2048；供应商下拉框中翻译专用模型（本地 HY-MT）会标注「不推荐用于总结」；超长会议的中间提取结果超过单次请求预算时逐级归并，最终请求不再随会议时长线性膨胀
- PDF 分页重绘更正：移除每页无意义的整文档克隆（此前每页白克隆一份从未使用），页脚位置按页面布局从毫米换算，不再用 40 设备像素这种贴边数值

## 2026-09-01
- 新增 GigaAM 多语言大模型识别引擎: 此前 GigaAM 后端只有俄语专用的 GigaAM-v3,现在设置页的 GigaAM 引擎下新增模型下拉框,可选 600M 参数的「GigaAM Multilingual (Large CTC)」——俄语保持一流水准,同时支持哈萨克语、吉尔吉斯语、乌兹别克语和英语。选多语言模型时源语言下拉框恢复可用(俄语版仍锁定 ru);缓存页能正确区分两个模型,下载也各自独立;`debug_pipeline.py --engine gigaam --asr-model gigaam-multilingual` 可直接验证。实测在 Apple Silicon 上走 MPS 加载约 3 秒,识别正常
- 修复 macOS 上悬浮窗与字幕窗口不能像 Windows 那样始终置顶的问题: 置顶提示在 macOS 上只对应「浮动层级 + 单一空间」,于是 Zoom 或浏览器一进全屏(全屏独占一个独立空间),悬浮窗就从画面里整个消失; 面板类窗口还可能随应用失活而隐藏。现在原生钉住窗口——层级高于浮动窗口但低于菜单栏、加入所有空间、可在全屏应用之上显示、禁止失活隐藏——并且每次显示后重新钉一次,行为对齐 Windows
- macOS 新增系统音频开关: 设置页的「音频」下拉框此前在 macOS 上被冻结成「禁用 (仅麦克风)」,想捕获 Zoom、浏览器或播放器的声音只能手动改 config.yaml —— 这个开关藏在配置文件里,不看文档根本不知道存在。现在下拉框可选「系统音频 (ScreenCaptureKit)」,悬停可查看屏幕录制权限的说明; 运行中切换会自动重建采集后端并清空 VAD 缓冲,新装用户默认选择系统音频
- 修复较新 macOS 上系统音频捕获一开就崩的问题: ScreenCaptureKit 回调方法的签名被 PyObjC 按全对象参数推断,而最后一个参数是 32 位枚举 —— 第一帧音频到达时桥接层把枚举值当指针解引用,整个进程段错误退出; 同时新版系统移除了 `exclusivelyCapturesSystemAudio` 标志、新版 PyObjC 的 dispatch 队列要求字节串标签、音频格式描述改为返回元组,三处叠加让 SCK 采集在新系统上从未启动成功过。实测修复后系统声音以 16kHz 单声道正常流入
- 修复 macOS 上系统音频切换失败会留下「假运行」状态的问题: 运行中从仅麦克风切到系统音频若失败(如屏幕录制权限被拒),之前直接丢弃旧后端; 现在会恢复原后端继续采集,与 Windows 侧切换失败的处理方式一致。失败同时会弹出本地化的原因与指引(如「需要屏幕录制权限……重启应用」),不再只写一条日志 —— 从 .app 启动时权限归属应用本体、首次必然未授权,此前这一步静默失败,界面一路显示 Running 却永远收不到声音
- 修复设置页「更新日志」里尖括号内容被吃掉的问题: 条目文本不转义直接拼进 HTML，而更新日志里恰恰引述着 `<think>…</think>` 这类代码原文——描述日志窗转义修复的那条，自己就在更新日志页里被吞掉了内容; 现在条目先转义再套加粗/代码标记
- 修复日志查看器里报错行丢失红色的问题: 翻译行高亮靠匹配「Translate:」这个字符串，但每条来自主记录器的日志行都带着「LiveTranslate: 」前缀、恰好以它结尾，于是所有这类行（包括 ERROR）都被刷成翻译蓝——打开日志窗想第一时间看到的报错红色反而消失了; 现在高亮只作用于 INFO 以下级别、并按消息的真实形状「Translate (412ms):」匹配，警告及以上永远保持级别色
- macOS 新增 `build_mac_app.sh`: 一条命令打包出可双击启动的 `LiveTranslate.app`（加 `--install` 同时安装进 /应用程序），以后从启动台或 Spotlight 打开，不用再开终端。包内的可执行文件是一段现场编译的 Mach-O 小启动器——LaunchServices 会以 -10669 拒绝 shell 脚本作主程序——由它带着正确的工作目录拉起 venv 里的 Python; 图标与应用内图标同一套设计。启动器两次构建字节级一致，重建 .app 后已授予的麦克风权限依然有效; 首次启动会重新索取麦克风权限，因为 TCC 现在把权限归属到 .app 而不是启动它的终端
- 修复悬浮窗「字幕」按钮与暂停状态没有任何视觉反馈的问题: 按钮的高亮样式靠字符串替换生成，但替换目标早在这套按钮样式改版时就已不存在，于是替换静默落空 —— 暂停时按钮配色与运行时完全相同，「字幕」按钮的文字又从不变化，开着还是关着全靠猜。现在暂停显示琥珀色、字幕开启显示绿色
- 修复停止后再启动，新会议记录沿用上一场统计的问题: 记录器在应用内只建一个实例，停止时关闭会话、再启动时复用同一实例开新会话，但条目数、语音时长和引擎信息没有随之重置 —— 第二场会议的元数据和结尾汇总会把上一场的数字全加进来，会议记录页里显示的条目数也是错的; 每场会话现在都从零开始统计
- 修复点「清空」后再导出总被提示「仅导出部分消息」的问题: 悬浮窗只保留最近 50 条，超出部分会记数、导出时据此提示「完整记录见会议文件」; 但清空消息列表时不重置这个计数，于是一次清空之后每次导出都会弹出误导性的截断提示，还指向一场早已不存在的旧会议记录文件。清空后重新计数
- 修复 macOS 系统音频持续解码失败时管线永久静音的问题: ScreenCaptureKit 的采集线程会把每条解码异常当偶发错误吞掉只记一条警告，而一旦流送来的是本解码器读不了的格式（按性质就是永久性的），此后每个缓冲都失败 —— 应用还在跑、界面还显示 Running、却再也不会有一帧音频; 麦克风与 Windows 采集路径本就有连续失败上限并升级为致命错误，现在 SCK 同样连续 20 次失败后按采集契约上报致命错误，由管线停止并提示
- 修复日志查看器吞掉尖括号内容的问题: 日志行直接拼进 HTML 再追加，Qt 会把 `<think>…</think>` 这类文本当标签吃掉 —— 而排查思考型模型问题时打开日志窗想看的恰恰就是这些行; 现在日志行按纯文本转义后渲染，一字不差
- 修复基准测试按钮可能永久失灵的问题: 某个模型配置缺少名称（或汇总阶段抛错）时，异常会顺着线程池传出来把整个基准测试线程打死，收尾的 `__DONE__` 永远发不出去 —— 而按钮正是靠它重新启用的，只能重启应用。现在无论哪一步失败都保证收尾，单个模型的错误也只记为该模型失败、不影响其余模型
- 修复新增模型对话框静默丢弃不完整条目的问题: 名称或模型 ID 留空时，对话框正常关闭，但列表处理逻辑会把这个条目整个丢掉 —— 用户填完表单、点了确定，什么都没发生。现在必填项留空会提示并留在对话框内
- 修复在悬浮窗改目标语言后，再改任意面板设置就会被改回去的问题: 改语言时写的是 `get_settings()` 返回的**副本**，文件存对了但面板自己的状态没跟上，下一次自动保存又把旧值写了回去; 面板新增 `update_setting()` 作为外部写入的唯一入口
- 修复字幕行列表在缺 `lines` 配置时显示与操作不一致: 列表用默认行填充，而增删改和上下移都退回到空列表，于是界面上有行、任何操作都对不上; 同时补齐「上移」缺失的上界检查
- 修复增量识别的回声去重从未生效: 提交的文本总是完整句子、总以句末标点结尾，而匹配时带着那个标点，新识别结果永远不以标点开头，因此这段去重逻辑一次都没触发过; 现在忽略尾部标点后可以识别真实回声，同时刻意保留单词级重复 —— 讲课时下一句以上一句的关键词开头很常见，删掉那个词会让句子失去主语，而重复一个词只是读起来啰嗦
- 修复 VAD 静音模式切换依赖无关设置项的问题: 从「自适应」切到「固定」时，静音阈值只在 `silence_duration` 恰好同时出现在设置字典里才会重算，否则会继续沿用自适应压低后的值
- 修复暂停后恢复会把两段语音拼成一句的问题: 暂停时 VAD 缓冲原封不动保留，恢复后的音频直接接在暂停前那半句后面，中间跨着任意长的时间跳切，输出一条前言不搭后语的字幕; 现在暂停会把在途的半句交给识别线程正常出字幕，缓冲清空，恢复后从干净状态开始
- 修复改任意设置都会重排全部字幕的问题: 控制面板每次自动保存都发送全量设置字典，样式因此被无条件重新应用 —— 满 50 条消息时每次约 59 毫秒，跑在界面线程上; 调一下 VAD 滑块或勾个复选框都会触发。现在样式没变就直接跳过
- 修复增量识别下短应答可能被静默丢弃的问题: 已缓冲的短句（如「да」「好的」）会先并入本次片段文本，再一起接受噪声过滤; 如果这次片段本身只有噪声而尾部又超过 2 秒，整条会被当作噪声丢掉 —— 用户说了话却什么都不出现。现在噪声过滤只判断本次片段自己的识别结果，早前已通过检查的缓冲内容照常输出
- 修复 ASR worker 意外退出后识别永久停止的问题: worker 被杀或崩溃后，客户端抛的是基类异常，而管线的恢复分支只捕「worker 已退出」这一类，于是每一段音频都撞同一个错误、永远不触发重启; 实测杀掉 worker 后 12 段只识别出 3 段并从此静默，修复后自动重启并识别出 11 段（只丢被杀瞬间正在处理的那一段）
- 修复会议中途切换翻译模型会让记录停止更新的问题: 切换时在途的那条翻译会被丢弃，但此前既不收尾转录也不收尾悬浮窗条目; 由于记录按说话顺序释放，那一条会把它之后的所有条目全部堵住（实测 3 条只写出 0 条），悬浮窗对应消息也永远停在「翻译中」

## 2026-08-31
- 修复模型基准测试与实时翻译在「禁用思考」上仍会分叉的问题: `Translator` 自身的 `no_think` 默认是 False，而每个调用方都传 True，因此任何直接构造 Translator 的代码（包括基准测试）在配置未显式设置 `thinking_style` 时会静默拿到「不发送任何参数」，而实时路径拿到的是「自动识别」—— 恰好是基准测试用来诊断的那个参数; 现在两条路径共用同一个构造入口 `translator_from_model_config()`，不再各自维护一份参数清单
- 补充配置说明: `config.yaml` 的空 api_key、上下文轮数、max_tokens 与采样温度都写明了实际语义；模型编辑对话框的「上下文轮数」提示补充了两个容易踩的点（提示词含 `{context}` 时改为嵌入而不发多轮；翻译专用模型对两种形式反应不同）；「模型连接超时」新增提示，说明首次请求通常明显更慢
- README 增加「排查问题」一节，介绍用 `debug_pipeline.py` 回放音频文件定位故障
- 本地 HY-MT 模型改用上下文多轮与贪心解码: 之前 `no_system_role` 的模型会**静默忽略** `context_turns`（设成 3 也没有任何上下文，且无提示），现在历史会以真正的多轮消息发送 —— 实测能正确还原代词指代（「现在求出它」→「现在求出这个函数的导数」），一整段课堂 8/8 稳定，prompt 仅多约 60 token; 注意这只在「多轮消息」形式下成立，把同样的历史贴进提示词文本仍会让模型续写（0/3）
- 本地模型改为贪心解码（temperature 0.7 → 0.0）: 同一句话重复翻译，之前 4 次里只有 2 次结果一致，现在 4/4 完全一致，质量相当且更快（平均 0.39s vs 0.49s）—— 字幕不该在同一句话上来回变。`top_k` 随之移除（贪心下无意义），`repetition_penalty` 保留作跑飞保护
- 修复本地 HY-MT 模型输出提示词而非译文的问题: 该模型是翻译专用模型而非指令模型，面对多行指令块会「续写」而不是执行 —— 实测长提示词 3/3 失败（吐回提示词、用光 128 token 预算、每句约 2.5 秒），改成单行指令后 4/4 正确（每句约 0.3 秒）; 同时把 `context_turns` 归零，因为在 `no_system_role` 下上下文只能经 `{context}` 注入，而带上下文块同样会触发跑飞（0/3）。仍是旧预设提示词的配置会自动迁移，自己改过提示词的不动
- 修复退出时等待本地模型服务约 5 秒的问题: 应用和设置面板各自持有一个服务管理器，负责停止的那个从未创建过该进程，因而无法回收它，只能轮询一个僵尸进程直到超时并误报「无法结束」; 现在共用同一个管理器，退出耗时从 7 秒降到 1 秒
- 修复本地服务启动后翻译器被重复构建两次的问题（上下文历史被清空两次）
- 修复空 API key 导致应用完全无法启动的问题: `config.yaml` 默认就是空 key，而新版 openai SDK 在构造客户端时直接拒绝空 key，于是任何未配置密钥的全新安装在窗口出现之前就崩溃; 现在空 key 会当作「本地服务不校验密钥」处理（LM Studio、Ollama、本地 MLX 服务都是这样）
- 修复会议记录条目顺序错乱的问题: 翻译在 8 个线程上并发完成，此前每条译文一到就写文件，导致 all/translation 记录的顺序是「翻译完成顺序」而不是「说话顺序」; 现在按说话顺序释放，退出时也会补写尚未等到译文的条目，不再丢失
- 新增「会议记录」设置页: 列出历次会议（时间、条数、时长、ASR 引擎、翻译模型），可预览内容、导出单场记录、删除单场记录
- 会议记录内容更完整: 每条附带语种与时长，新增 Markdown 版记录，会话结束写入摘要（起止时间、总时长、语音时长、条数、使用的引擎与模型），并生成 JSON 元数据便于列表展示
- 新增调试系统: `debug_pipeline.py` 可把音频文件当作采集源喂进真实管线（VAD → ASR worker → 翻译 → 转录），完整捕获 DEBUG 日志并输出分阶段报告; 「某一环节被要求工作却什么都没产出」会判定为失败，而不是安静通过
- 修复 ASR worker 在管线启动前加载完成会被误判为「已被更新的切换取代」而丢弃的问题
- 修复退出时控制面板的后台线程（缓存扫描、MLX 探测）可能在仍在运行时被销毁的问题
- 退出日志不再重复打印两次「Pipeline stopped」

## 2026-08-30
- 修复退出可能挂起或留下残留进程的问题: 停止流程此前用阻塞式队列写入投递停止信号,队列满且识别线程已退出时会永远等待; 现在停止是幂等且有界的,任一清理步骤失败也会继续回收后续的线程、转录文件、识别 worker 和本地服务
- 修复识别 worker 正在长耗时转写时,退出或切换引擎要等最长 120 秒的问题: 客户端的请求锁与生命周期锁此前是同一把,退出被阻塞在进行中的转写后面; 现在退出会立即中止 worker,界面在一两秒内响应; 模型加载中途切换引擎同样可以立即中止
- 修复识别 worker 已消失但状态仍显示"就绪"的问题: 进程自行退出或句柄已关闭时会被误判为可复用,后续请求既不恢复也不报错
- 修复"删除全部模型并退出"可能删不干净的问题: 此前直接退出 Qt 绕过了流水线清理,识别 worker 仍持有模型目录; 现在先完整停止并确认 worker 已退出再删除,删除在后台进行并显示进度,失败路径会在界面上列出
- 修复 Ctrl-C 在信号处理器里执行数秒清理的问题, 连按两次不再产生重复清理
- 修复启动后 500 毫秒内点击暂停或退出会被延迟启动回调覆盖的问题; 托盘与悬浮窗在真正启动前不再显示"运行中"
- 修复选中 HY-MT 模型时点击模型列表卡顿约 2 秒的问题: 部署状态、环境状态和服务状态此前在界面线程上同步派生子进程和发起 HTTP 请求; 现在全部在后台线程完成,界面只读缓存值
- 修复 HY-MT 服务未启动时每 5 秒清空一次翻译上下文的问题: 此前每轮探测都会重置翻译器; 现在只在"可用→不可用"的状态跳变时执行一次,自动重启也有退避和次数上限,达到上限后提示手动启动
- 修复模型下载静默期间取消无效的问题: 取消此前只在子进程输出新行时才被检查,长时间无输出的下载无法中断; 现在取消随时生效并终止整个进程树,不再残留进程或 pid 文件
- 修复下载过程中退出后控制台不再输出日志的问题: 被替换的 sys.stderr 只在成功分支恢复,中途退出会让它永久指向已销毁的窗口; 现在任何退出路径都会恢复
- 修复准备 HY-MT 模型会向应用自身运行环境安装依赖、并可能删除服务正在使用的模型目录的问题
- 修复首次启动向导写入的 VAD 阈值与 config.yaml 不一致的问题 (0.3 vs 0.5)
- 修复连续短句字幕丢失中间句的问题: 字幕窗口此前收到新句子会取消待显示的上一句,现在按到达顺序排队显示
- 修复重复输出检测受流式开关影响的问题: 非流式路径此前跳过检测; 同时新增对"开头正常、后面开始复读"的尾部检测
- 修复翻译服务不可达时每句要等两倍超时的问题: 流式请求的失败回退此前捕获所有异常并整句重试
- 修复增量识别把同一句短应答重复输出的问题: 短句被缓冲时不推进音频裁剪与回声去重状态,下一轮会重新识别出同样的内容
- 修复噪声片段被丢弃后,残留的增量状态串到下一段语音的问题
- 修复删除模型后配置、悬浮窗和运行中的翻译器可能指向不同模型的问题: 删除活动模型之前的条目会使索引整体前移
- 修复模型基准测试与实际翻译请求不一致的问题: 现在复用同一套请求组装逻辑(思考模式、覆盖参数、extra_body、JSON 模式、角色拆分); 对思考模型或不可达端点跑基准不再崩溃或耗时翻倍
- 修复长字幕换行在界面线程上的二次方开销,以及某个目标语言译文为空时出现前导分隔符
- 修复反复切换到缓存页会并发派生多个模型目录扫描线程的问题
- 修复导出的对话记录在超过 50 条后静默不完整的问题: 现在明确提示只包含最近 N 条,并给出完整转录文件路径
- 修复 Windows 音频采集在队列拥塞或设备重启失败时静默停止出声的问题: 现在丢弃最旧的音频块而不是让采集线程退出,重启失败会重试并在耗尽次数后明确停止
- 修复 macOS 仅麦克风模式下终止性采集失败被当作"暂时没有数据"的问题; 三个采集后端现在对"无数据"与"终止性失败"的表达一致
- 远程 ASR 服务默认改为只监听 127.0.0.1(此前 0.0.0.0 直接暴露无鉴权的 GPU 推理端口),新增可选的共享密钥校验和请求体大小上限; `uvicorn asr_server:app` 不再因缺少配置而启动失败
- Fun-ASR-Nano 音频加载失败现在保留原始错误,不再被后续的 UnboundLocalError 掩盖
- HY-MT 服务的全部提示与错误文案、语言切换提示改为跟随界面语言
- 移除 macOS 依赖中未被使用的 pyannote-audio / torchcodec(仅长音频转写路径需要,本项目不走该路径); 新增依赖双向对称性检查,防止再出现单平台缺失

## 2026-08-29
- 修复选中本地 HY-MT 模型但其服务未启动时,字幕永久停留在"翻译中"的问题: 此前该失败与"程序正在退出"共用同一种异常被静默跳过,消息既不会落定也不会写入合并转录文件,且内部待处理表随会话时长无限增长; 现在会显示明确的错误提示并正常收尾
- 修复 ASR 后端返回异常结果时识别永久停止的问题: 此前结果字段缺失或类型错误会让 ASR 线程带异常退出,采集线程仍在向无人消费的队列写入,界面不再出现新字幕且不触发任何重启逻辑; 现在统一校验结果格式,单条坏结果只丢弃自身并记录日志,后续音频继续识别
- 修复远程 ASR 服务不可用时被当作"没有语音"的问题: 服务宕机、连接超时、HTTP 5xx、返回非 JSON 现在都会进入与本地 worker 故障相同的恢复流程并显示 ASR 不可用,而不是静默丢弃音频; 合法的空识别结果仍然安静跳过
- 修复 macOS 上改动任意设置都会中断音频约 8 秒的问题: 控制面板每次自动保存都会发送全量设置,而系统音频采集未做设备相等性判断,导致设备毫无变化时也整流重启; 现在仅在设备真正改变时才切换,切换失败会恢复原设备,无法恢复时明确停止并提示
- 修复说话过程中改设置或切换 ASR 引擎可能损坏语音缓冲的问题: 界面线程绕过锁重置 VAD 状态,与采集线程并发写入时可造成缓冲区与置信度序列长度失配,进而影响切分点选择和静音判定
- 修复增量 ASR 分句库缺失时识别线程直接退出的问题: 现在降级为不分句并记录明确错误,不再拖垮整条识别链路
- `requirements.txt` / `requirements-mac.txt` 现在可独立装出可运行环境: 补充此前仅由安装脚本单独安装的 yasbd-lib、Windows 侧缺失的 socksio(socks5 代理需要),并为 transformers 补上与本地 MLX 服务一致的 <5 上界
- 修复测试与发布流程: 启动器提示文案回归导致测试失败、CI 依赖不全导致部分测试无法收集、Windows 发布作业未依赖测试作业(测试失败仍会发布); 现在两个平台的发布均受测试门禁约束,并新增断言防止该门禁再次缺失

## 2026-08-22
- 新增 macOS 13+ Apple Silicon 支持说明，以及运行离线平台/音频测试的原生 arm64 CI job。
- 新增 macOS arm64 源码包产物（`LiveTranslate-macos-arm64-*.tar.gz`）；签名和公证仍属于后续发布工作。
- 音频诊断默认执行无需设备的格式归一化 smoke test；Windows WASAPI 探测改为显式使用 `--live-windows`。
- 补充 ScreenCaptureKit/麦克风权限、CTranslate2 仅 CPU、MPS 预期和未签名 app 的 TCC 限制说明。
- 明确 GigaAM-v3 集成方式：官方 Hugging Face `e2e_rnnt` revision、俄语 ASR 范围、约 25 秒短音频限制，以及尚未接入应用的官方长音频支持。

## 2026-08-17
- 增量 ASR 的分句库从 PySBD 切换到 yasbd-lib (#37): API 兼容无行为变化, 新增韩语等 17 种语言的原生分句规则 (此前韩语回退英语规则), 长文本分割速度大幅提升
- 修复 DeepSeek 默认开启思考模式导致翻译返回空、界面显示原文的问题 (#38): DeepSeek 需要 thinking.type=disabled 才能关闭思考, 此前发送的 enable_thinking=false 是 Qwen 风格参数对其无效; 模型编辑对话框新增「禁用思考」风格选择 (自动识别 / DeepSeek·火山方舟·GLM / Qwen·百炼·硅基流动 / vLLM·SGLang 自部署 / OpenAI·Grok reasoning_effort / 不发送), 自动识别对 OpenAI 等会拒绝未知参数的官方端点不再乱发参数; 思考耗尽 token 返回空时输出诊断日志
- 修复高 DPI 缩放 (150%+) 下设置窗口超出屏幕、确定/取消按钮点不到的问题 (#39): 模型编辑对话框与控制面板各标签页改为按需滚动, 窗口高度自动限制在屏幕可用范围内
- 安装与更新不再向系统盘写入下载缓存: 便携版的 uv 缓存和托管 Python 移入程序目录, install/update 脚本的 pip 缓存与临时文件移入项目目录, 装完自动清理

## 2026-07-11
- 修复 Fun-ASR-Nano 首次加载时 Qwen3-0.6B 权重下载被 180 秒超时打断的问题 (#32): 权重改为在模型下载阶段提前获取, ASR worker 启动不再等待大文件下载
- 修复 ModelScope 1.38+ 缓存位置变化导致模型漏检的问题 (#32, #33): 兼容各版本 SDK 的全部缓存目录布局, 升级 SDK 后不再重复下载已有模型
- Qwen3-0.6B 权重支持从 ModelScope 下载, ModelScope 源用户不再被迫走 HuggingFace (#33)

## 2026-06-20
- 新增「远程 Whisper」ASR 引擎: 把语音识别外包到带 GPU 的另一台机器 (附 `asr_server.py` 服务端), 无 GPU 的机器也能实时识别
- 新增「WebID 身份核验」翻译提示词预设: 针对视频核验场景调优用词
- ASR 改为子进程隔离运行: worker 崩溃/超时自动重启, 内存增长到阈值自动回收, 识别故障不再拖垮界面
- 字幕窗口支持鼠标穿透 (#28): 字幕设置里新增开关, 托盘菜单可「字幕鼠标穿透」快速切换, 开启后点击直达背后窗口 (此时中键拖动失效, 先关穿透再移动)

## 2026-05-10
- 新增「导出到文件」: 支持原文 / 译文 / 原文+译文 三种格式, 悬浮窗右键菜单与托盘菜单均可触发
- 新增「转录持久化」(默认开启): 每次会话自动在 `transcripts/` 下创建 3 份文件 (原文 / 译文 / 全部), 每段识别结果实时追加写入, 不再受悬浮窗 50 条上限丢失早期文本
- 设置面板「缓存」标签新增「转录持久化」分组: 开关 + 打开转录文件夹按钮
- 新增内存占用上限保护: RSS 超过 4096MB 时弹托盘通知提示重启 (ASR 后端存在 native 侧工作区/缓存, Python GC 与 `torch.cuda.empty_cache` 不一定能回收)
- 日志新增 `MEM[asr#/tick]` 行: 每次真实 ASR 调用后输出 RSS / GPU(alloc/reserved) / 音频时长 / overlay 消息数 / VAD buffer 长度, 便于排查内存问题

## 2026-04-20
- 移除 Qwen3-ASR 引擎 (ONNX + GGUF 方案兼容性较差, 相关模型文件和 llama.cpp 运行时依赖一并清理)
- 模型配置新增「高级参数」: `temperature` / `top_p` / `max_tokens` / `frequency_penalty` / `presence_penalty` / `seed`, 每项独立「覆盖」开关, 未勾选时使用服务端默认值
- 模型配置新增 `extra_body` (JSON): 供应商专有参数透传, 如 `thinking_budget`、`reasoning_effort` 等, 保存时自动校验 JSON 格式
- 修复 Anime-Whisper 模型未缓存时下载对话框静默无动作的问题
- 修复设置面板「更新日志」Tab 空白 (正则匹配 H3 但文件用 H2, 3 月加入功能起就失效)

## 2026-04-18
- 新增 ASR 引擎: Anime-Whisper (litagin/anime-whisper), 日语动画/Galgame 特化, 擅长识别喘息/叹息等非语言发声
- 修复 HF 缓存检测误判: 下载中断留下的空目录不再被认为"已缓存"

## 2026-03-31
- 管道线程拆分: 原 capture+VAD+ASR 单线程改为 capture 与 ASR 两条独立线程, 长句 ASR 不再阻塞 RMS/VAD 条实时更新
- ASR 调度新增有界队列 (16 段), 队列满时丢弃最旧的 interim 段, 避免堆积导致延迟

## 2026-03-26
- 默认翻译提示词优化: 新增 ASR 纠错规则 (识别错别字/同音字时尽量按上下文修正)、流畅性规则 (避免逐字直译)

## 2026-03-25
- 样式标签新增「重置窗口位置」按钮: 字幕窗口回到 (100,100), 主悬浮窗回到屏幕右下角
- 字幕窗口默认位置从屏幕底部居中改为 (100,100), 最小高度调整为 200px
- 窗口位置恢复时校验是否在可见屏幕范围内 (使用 availableGeometry 排除任务栏), 高度变化后 clamp 到屏幕边界

## 2026-03-24
- 字幕窗口: 超长文本自动换行显示(不再分段), 背景高度平滑动画, 文字渲染 Pixmap 缓存
- 主悬浮窗/字幕窗口: 位置和大小自动记忆, 重启后恢复
- 主悬浮窗: 精简模式切换动画
- 设置: 移除配置文件 key 白名单限制

## 2026-03-23
- 品牌更名 LiveTrans → LiveTranslate
- 模型配置新增: 流式传输、结构化输出、上下文数、禁用思考(默认开启)
- 翻译结果流式逐字显示
- 提示词优化: 禁止多候选翻译, 编辑即时生效
- 模型重复输出检测与提示
- ASR 引擎标注[准确]/[快速]
- 设置面板新增更新日志 Tab
