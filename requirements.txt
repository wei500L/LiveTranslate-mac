# Core dependencies
# Keep NumPy within the range supported by current Numba wheels. Without this
# cap, uv can backtrack to Numba/llvmlite releases that do not support Python 3.12.
numpy>=1.24.0,<2.3
numba>=0.59.0
PyYAML>=6.0
httpx>=0.28.0
openai>=1.0.0
psutil>=5.9.0

# Audio capture (Windows WASAPI loopback)
PyAudioWPatch>=0.2.12

# ASR engines
faster-whisper>=1.0.0
funasr>=1.3.28
omegaconf>=2.3.0
kaldiio>=2.18.0
torch-complex>=0.4.0
soundfile>=0.12.1
librosa>=0.10.0
jaconv>=0.3.0
jamo>=0.4.1
hydra-core>=1.3.2
six>=1.16.0
sentencepiece>=0.2.0
tiktoken>=0.7.0
transformers>=4.40.0

# VAD (bundles the Silero v5 model in the wheel — no GitHub/torch.hub download)
silero-vad>=5.0

# Model hubs
modelscope>=1.20.0
huggingface_hub>=0.20.0

# UI
PyQt6>=6.5.0
pyqtdarktheme>=2.1.0

# torch & torchaudio - install separately with CUDA support:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
#   Blackwell GPUs (RTX 50xx, sm_120) require cu128:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
